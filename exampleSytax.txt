// conditional statement: if, if-else, else, nested if, combined, elif
if input is less than 0 then, display 'If statement'.

// If

    if (num > 0) {
        do: 
            print("The number is positive.");
}

// If...else
    if (num >= 0) {
    do:
        print("The number is positive or zero.");
} else {
    do:
        print("The number is negative.");
}

// Nested if...else
if (age >= 18) {
    do:
        print("You are an adult.");
    if (age >= 65) {
        do:
            print("You are also eligible for senior citizen benefits.");
    } else {
        do:
            print("You are not yet eligible for senior citizen benefits.");
    }
} else {
    do:
        print("You are a minor.");
}

// if...elif...else
if (score >= 90) {
    do:
        print("You got an A!");
} elif (score >= 80) {
    do:
        print("You got a B!");
} elif (score >= 70) {
    do:
        print("You got a C!");
} else {
    do:
        print("You failed the test.");
}

//True
condition(isRaining == True) {  
    do:  
        print("It's raining, take an umbrella.");  
    or:  
        print("It's not raining, enjoy your day!");  
}  

//False
condition(isWeekend == False) {  
    do:  
        print("It's a weekday, time to work.");  
    or:  
        print("It's the weekend, relax!");  
}  

//null
condition(userName == null) {
    do:
        print("No user name provided.");
    or:
        print("Hello, " + userName + "!");
}

//and, or, not
a = True
b = False

print(“a” and “b”); 
print(“a” or “b”);
print(not “a”);    


//in
condition(“s” in “sequence”) {
    do: 
        print(“the char s is in the string sequence”);
}

//condition, do, or, else
condition(num > 0) {
    do: 
        print("Num is greater than 0");
    or:
        print("Num is a negative integer");
    else:
        print("num is 0");
}

//break, continue
loop(counter = 0; counter < 5; counter = counter + 1) {
    condition(counter == 3) {
        do:
            print("Skipping number 3");
            continue;
    }
    condition(counter == 4) {
        do:
            print("Breaking at number 4");
            break;
    }
    do:
        print("Counter is: " + counter);
}


//switch, case, default
switch(dayOfWeek) {
    case "Monday": 
        do: 
            print("Start of the work week!");
            break;
    case "Friday": 
        do: 
            print("End of the work week!");
            break;
    case "Saturday": 
    case "Sunday": 
        do: 
            print("It's the weekend!");
            break;
    default: 
        do: 
            print("Just another weekday.");
}

//loop
try:
        loop(num2 == 0) {
          do:
            interrupt("Cannot divide by zero!");
        }
        return num1 / num2;
    except:
        print("An error occurred during division.");


//function
function greet() {
        return "Hello, my name is " + this.name + " and I am " + this.age + " years old.";
    }

//class
class Person {
    function initialize(name, age) {
        this.name = name;
        this.age = age;
    }
}

//return
try:
        loop(num2 == 0) {
          do:
            interrupt("Cannot divide by zero!");
        }
        return num1 / num2;
    except:
        print("An error occurred during division.");

//try, except, interrupt
loop(counter = 0; counter < 5; counter = counter + 1) {
    try:
        loop(counter == 3) {
            do:
                interrupt("Error: Counter cannot be 3!");
        }
        loop(counter == 4) {
            do:
                interrupt("Error: Counter cannot be 4!");
        }
        do:
            print("Counter is: " + counter);
    except:
        print("An error occurred, skipping this iteration.");
        continue;
}

// Arithmetic Expression 
a = 5
b = 3
result = a + b  // addition
print("The result of addition is: ", result)

result = a - b  // subtraction
print("The result of subtraction is: ", result)

result = a * b  // multiplication
print("The result of multiplication is: ", result)

result = a / b  // division
print("The result of division is: ", result)

// Comparison Expression 
num1 = 10
num2 = 20

if num1 < num2:
    print("num1 is less than num2")  // True

if num1 == num2:
    print("num1 is equal to num2")  // False

if num1 != num2:
    print("num1 is not equal to num2")  // True

// Logical Expression 
a = True
b = False

if a and b:
    print("Both are true")
elif a or b:
    print("At least one is true")
else:
    print("Neither are true")

if not a:
    print("a is False")  // False

// String Expression 
name = "Ashley"
greeting = "Hello, " + name + "!"  // Concatenation
print(greeting)

message = "Welcome to the world of coding!"
print(message[0:7])  // Slicing: "Welcome"

length = len(message)  // Length of the string
print("Length of the message: ", length)

// Function Call Expression 
def square(num):
    return num * num

result = square(5)  // Function call expression
print("The square of 5 is: ", result)

// List Expression 
numbers = [1, 2, 3, 4, 5]

# Adding an item to the list
numbers.append(6)
print("List after appending 6: ", numbers)

# Accessing an item by index
print("First item: ", numbers[0])

# Slicing the list
print("Sliced list: ", numbers[1:4])

# Removing an item from the list
numbers.pop(2)
print("List after popping: ", numbers)
